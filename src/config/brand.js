/**
 * Nome exibido na sidebar/tela de login do admin — configurável via .env
 * (VITE_APP_SHORT_NAME / VITE_APP_NAME), sem precisar editar código.
 */
export const APP_SHORT_NAME = import.meta.env.VITE_APP_SHORT_NAME || 'G4';
export const APP_NAME       = import.meta.env.VITE_APP_NAME || 'G4 Business';
